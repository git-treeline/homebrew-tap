# typed: false
# frozen_string_literal: true

# This file was generated by GoReleaser. DO NOT EDIT.
class GitTreeline < Formula
  desc "Worktree environment manager — isolated ports, databases, and services across parallel development environments"
  homepage "https://github.com/git-treeline/cli"
  version "0.52.1"
  license "MIT"

  on_macos do
    if Hardware::CPU.intel?
      url "https://github.com/git-treeline/cli/releases/download/v0.52.1/git-treeline_0.52.1_darwin_amd64.tar.gz"
      sha256 "8a23453e91fe80ae995e5af9e78841733045ab1917fee5e34e0fb8be82b969af"

      define_method(:install) do
        bin.install "git-treeline"
        bin.install_symlink "git-treeline" => "gtl"

        # Completions target the 'gtl' command name (the symlink users invoke),
        # which is what 'git-treeline completion' now emits (#compdef gtl / _gtl).
        generate_completions_from_executable(bin/"git-treeline", "completion", base_name: "gtl")
      end
    end
    if Hardware::CPU.arm?
      url "https://github.com/git-treeline/cli/releases/download/v0.52.1/git-treeline_0.52.1_darwin_arm64.tar.gz"
      sha256 "4a8d8a1c83cb11fc774915d5232bab2e816ab3819030cb4eb5e9ac32d04f0b8d"

      define_method(:install) do
        bin.install "git-treeline"
        bin.install_symlink "git-treeline" => "gtl"

        # Completions target the 'gtl' command name (the symlink users invoke),
        # which is what 'git-treeline completion' now emits (#compdef gtl / _gtl).
        generate_completions_from_executable(bin/"git-treeline", "completion", base_name: "gtl")
      end
    end
  end

  on_linux do
    if Hardware::CPU.intel? && Hardware::CPU.is_64_bit?
      url "https://github.com/git-treeline/cli/releases/download/v0.52.1/git-treeline_0.52.1_linux_amd64.tar.gz"
      sha256 "3f9aec6e2694a1778f4f69065e5768892a7d40dae235ec8fcf92bb0532ed69e5"
      define_method(:install) do
        bin.install "git-treeline"
        bin.install_symlink "git-treeline" => "gtl"

        # Completions target the 'gtl' command name (the symlink users invoke),
        # which is what 'git-treeline completion' now emits (#compdef gtl / _gtl).
        generate_completions_from_executable(bin/"git-treeline", "completion", base_name: "gtl")
      end
    end
    if Hardware::CPU.arm? && Hardware::CPU.is_64_bit?
      url "https://github.com/git-treeline/cli/releases/download/v0.52.1/git-treeline_0.52.1_linux_arm64.tar.gz"
      sha256 "e56b11a1117be365be87964f57fba6ce8b26b1bb7d1325c61f7dd00d40f9b6e1"
      define_method(:install) do
        bin.install "git-treeline"
        bin.install_symlink "git-treeline" => "gtl"

        # Completions target the 'gtl' command name (the symlink users invoke),
        # which is what 'git-treeline completion' now emits (#compdef gtl / _gtl).
        generate_completions_from_executable(bin/"git-treeline", "completion", base_name: "gtl")
      end
    end
  end

  def post_install
    # If the user previously installed the HTTPS router service, bounce it
    # so the launchd-managed process picks up the new gtl binary. The
    # --if-installed flag makes this a no-op for users who never ran
    # 'gtl serve install', so first-time brew installs stay quiet.
    system bin/"gtl", "serve", "restart", "--if-installed"
  end

  def caveats
    <<~EOS
      Get started:
        cd your-project && gtl install

      One command: config, hooks, ports, env, and optional HTTPS routing.
      Docs: https://git-treeline.dev/docs/getting-started/
    EOS
  end
end
